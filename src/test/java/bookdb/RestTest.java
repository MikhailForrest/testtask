package bookdb;

public class RestTest /* extends JerseyTest */ {

	/*
	 * @Override protected Application configure() { ApplicationContext context
	 * = new AnnotationConfigApplicationContext(SpringConfig.class); return new
	 * AppConfig().property("contextConfig", context); }
	 * 
	 * @Test public void BooksTest() { final WebTarget target = target("books");
	 * final Response response = target.request(MediaType.TEXT_HTML).get();
	 * assertEquals(200, response.getStatus()); }
	 * 
	 * @Test public void BookIdTest() { final WebTarget target =
	 * target("book/0"); final Response response =
	 * target.request(MediaType.TEXT_HTML).get(); assertEquals(200,
	 * response.getStatus()); }
	 * 
	 * @Test public void PostTest() { boolean b1 = false; final WebTarget target
	 * = target("book"); JSONObject bookO = new JSONObject(); try {
	 * bookO.put("isbn", "1000001").put("title", "title1").put("price",
	 * "0D").put("description", "description11111"); final Response response =
	 * target.request().post(Entity.entity(bookO, MediaType.APPLICATION_JSON));
	 * assertEquals(400, response.getStatus()); } catch (Exception e) {
	 * e.printStackTrace(); b1 = true; } assertFalse(b1); }
	 * 
	 * @Test public void PutTest() { boolean b1 = false; final WebTarget target
	 * = target("book/0"); JSONObject bookO = new JSONObject(); try {
	 * bookO.put("isbn", "1000001").put("title", "title1").put("price",
	 * "0D").put("description", "description11111"); final Response response =
	 * target.request().put(Entity.entity(bookO, MediaType.APPLICATION_JSON));
	 * assertEquals(400, response.getStatus()); } catch (Exception e) {
	 * e.printStackTrace(); b1 = true; } assertFalse(b1); }
	 */
}
